import React from 'react';
import Header from "../components/Header/Header";

const ParserPage = () => {
    return (
        <div>
            <Header/>
        </div>
    );
};

export default ParserPage;